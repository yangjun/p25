'use strict';
var app = angular.module('wxApp');

/**
 * 回答：所有回答
 */
app.controller('FaqAnswerListCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);

/**
 * 回答：添加回答
 */
app.controller('FaqAnswerCreateCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);

/**
 * 回答：编辑回答
 */
app.controller('FaqAnswerEditCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);
