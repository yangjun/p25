'use strict';
var app = angular.module('wxApp');

/**
 * 处方：所有处方
 */
app.controller('PrescListCtrl', ['$rootScope', '$scope', 'prescService',
    function ($rootScope, $scope, prescService) {
    }]);

/**
 * 处方：处方信息
 */
app.controller('PrescInfoCtrl', ['$rootScope', '$scope', 'prescService',
    function ($rootScope, $scope, prescService) {
    }]);

/**
 * 处方：添加处方
 */
app.controller('PrescCreateCtrl', ['$rootScope', '$scope', 'prescService',
    function ($rootScope, $scope, prescService) {
    }]);

/**
 * 处方：编辑处方
 */
app.controller('PrescEditCtrl', ['$rootScope', '$scope', 'prescService',
    function ($rootScope, $scope, prescService) {
    }]);
