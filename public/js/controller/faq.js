'use strict';
var app = angular.module('wxApp');

/**
 * 问答：所有问答
 */
app.controller('FaqListCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);

/**
 * 问答：问答信息
 */
app.controller('FaqInfoCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);

/**
 * 问答：添加问答
 */
app.controller('FaqCreateCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);

/**
 * 问答：编辑问答
 */
app.controller('FaqEditCtrl', ['$rootScope', '$scope', 'faqService',
    function ($rootScope, $scope, faqService) {
    }]);
